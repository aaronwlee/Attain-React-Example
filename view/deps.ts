const useWindow: any = window;

const React: any = useWindow.React;
const ReactDOM: any = useWindow.ReactDOM;
const styled: any = useWindow.styled;
const ReactRouterDOM: any = useWindow.ReactRouterDOM;

export { React, ReactDOM, styled, ReactRouterDOM };
